# Ongoing state: copy to local/pki.env after setup-intermediate.yml has run.
# env.sh sources this automatically on every Terraform run.
export TF_VAR_enable_pki=true
export TF_VAR_pki_intermediate_ready=true
