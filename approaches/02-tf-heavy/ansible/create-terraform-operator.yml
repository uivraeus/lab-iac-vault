---
- name: Create terraform-operator AppRole
  hosts: all
  become: false
  vars:
    vault_tokens_policy_file: "{{ playbook_dir }}/files/terraform-operator-policy.hcl"
    vault_tokens_output_dir: "{{ playbook_dir }}/../../../local"
  roles:
    - role: vault_tokens
